# finalProject

Running the code is fairly simple. Running the code opens a GUI where you 
can interact with all the chart elements. Both X and Y inputs must have the 
same amount of numbers. The 'CLEAR' button resets all of the user inputed
eteries and resets the graph to start over. There must be variables inputed
for a graph to appear o